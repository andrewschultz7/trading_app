CREATE TABLE `trading_1min` (
  `Timestamp` INT,
  `gmtoffset` INT,
  `Datetime` DATETIME2,
  `Open` DECIMAL(10,2),
  `Close` DECIMAL(10,2),
  `High` DECIMAL(10,2),
  `Low` DECIMAL(10,2),
  `Volume` INT
);
